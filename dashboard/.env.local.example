# Base URL of the Visitor Analytics backend (no trailing slash).
BACKEND_URL=http://localhost:3000
# API key used by the dashboard to query the backend. Kept server-side only;
# it is never shipped to the browser.
BACKEND_API_KEY=va_test_key_1
